# Hide automation folder and its runtime artifacts.
# The actual orchestrator scripts are in .scripts/ (dot-folders are hidden
# by the GitHub web UI), and must be tracked so CI can execute them.
automation/
.scripts/*.log
.scripts/state.json
*.vvp
